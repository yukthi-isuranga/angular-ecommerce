import { ViewPanal } from './view-panal';

describe('ViewPanal', () => {
  it('should create an instance', () => {
    const directive = new ViewPanal();
    expect(directive).toBeTruthy();
  });
});
